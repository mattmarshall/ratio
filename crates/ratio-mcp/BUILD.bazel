load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//visibility:public"])

exports_files(["Cargo.toml"])

rust_library(
    name = "ratio-mcp",
    srcs = glob(["src/*.rs"]),
    crate_name = "ratio_mcp",
    crate_root = "src/lib.rs",
    edition = "2021",
    deps = [
        "//crates/ratio-chart",
        "//crates/ratio-kernel",
        "//crates/ratio-rules",
        "//crates/ratio-store",
        "@crates//:anyhow",
        "@crates//:serde",
        "@crates//:serde_json",
    ],
)

rust_test(
    name = "ratio-mcp_test",
    crate = ":ratio-mcp",
    edition = "2021",
)
