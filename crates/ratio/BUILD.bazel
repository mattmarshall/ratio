load("@rules_rust//rust:defs.bzl", "rust_binary")

package(default_visibility = ["//visibility:public"])

exports_files(["Cargo.toml"])

rust_binary(
    name = "ratio",
    srcs = ["src/main.rs"],
    edition = "2021",
    deps = [
        "//crates/ratio-api",
        "//crates/ratio-common",
        "//crates/ratio-kernel",
        "//crates/ratio-tui",
        "//proto:ratio_rust_proto",  # LedgerServer (ratio_proto)
        "@crates//:anyhow",
        "@crates//:tokio",
        "@crates//:tonic",
    ],
)
