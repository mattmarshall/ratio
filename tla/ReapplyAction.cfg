\* No record of having applied. A retry re-applies, the cost is unchanged so
\* the books still tie, and the position doubles.
\* AppliedAtMostOnce MUST go red.
SPECIFICATION Spec
CONSTANTS
    ActionIds = {a1}
    Days      = {1, 2}
    RecordApplied      = FALSE
    ApplyInExDateOrder = TRUE
INVARIANTS
    TypeOK
    AppliedAtMostOnce
