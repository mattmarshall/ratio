\* The engine as designed: each sale ranks the holding at ITS price.
\* Same two lots, two prices, two answers. A sort-and-walk cannot do this.
SPECIFICATION Spec
CONSTANTS
    TreatAsOrder = FALSE
INVARIANTS
    TypeOK
    TheLotTakenMinimisesTax
    TheTwoPricesDisagree
