\* A publication key exposes a bootstrap before every referenced object exists.
\* PublishedBookComplete MUST go red.
SPECIFICATION Spec
CONSTANTS
    Creators = {alice, bob}
    NoBook = noBook
    ConditionalPublication = TRUE
    RequireComplete = FALSE
    KeepGeneration = TRUE
    RefuseIncomplete = TRUE
    GrantOnlyPublished = TRUE
INVARIANTS
    TypeOK
    PublishedBookComplete
