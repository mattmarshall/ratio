\* The engine as designed: a relief that finds the factor moved under it is
\* sent back to be recomputed rather than written.
SPECIFICATION Spec
CONSTANTS
    Sales = {s1, s2}
    MaxSplits = 2
    PinTheFactor   = TRUE
    RefuseOnChange = TRUE
PROPERTIES
    EverySaleIsEventuallyWritten
INVARIANTS
    TypeOK
    ReliefIsWrittenAgainstWhatItRead
    WrittenMeansConverted
