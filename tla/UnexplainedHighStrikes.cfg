\* AStrikeIsNotTakenOverABlockingBreak MUST go red.
\* The gate the console reported and nothing enforced: `STATE_BLOCKED` on the
\* screen, and `ratio strike` never asking.
SPECIFICATION Spec
CONSTANTS
    Breaks = {b1, b2}
    Figures = {0, 1, 2}
    Facts = {f1}
    MaxPos = 2
    Threshold = 2
    RefuseOnUnexplainedHigh = FALSE
    RetireOnANewFigure = TRUE
    PinExplanationToThePrefix = FALSE
INVARIANTS
    TypeOK
    AStrikeIsNotTakenOverABlockingBreak
