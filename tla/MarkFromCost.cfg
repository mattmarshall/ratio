\* Marking from COST. Every mark posts the whole gain again, and the entry is
\* still balanced — so the trial balance ties while the book is wrong.
\* MarkLandsOnMarket MUST go red.
SPECIFICATION Spec
CONSTANTS
    Units  = {1}
    Prices = {1, 2}
    Days   = {1, 2}
    MarkFromCarrying = FALSE
CONSTRAINT
    StateBound

PROPERTIES
    MarkLandsOnMarket
