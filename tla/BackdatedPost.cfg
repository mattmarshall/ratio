\* AClosedPeriodRefusesABackdatedPost MUST go red.
\* The door Stage 1 named and nothing enforced: a close recorded, and
\* `Journal::append` never asking the closed-through day.
SPECIFICATION Spec
CONSTANTS
    Views = {v1}
    Days = {1, 2}
    MaxPos = 2
    RefuseOnClosedPeriod = FALSE
INVARIANTS
    TypeOK
    AClosedPeriodRefusesABackdatedPost
