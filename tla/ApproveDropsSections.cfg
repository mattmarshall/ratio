\* The bug that shipped: approve writes back only the rules.
\* ApprovePreservesSections MUST go red.
SPECIFICATION Spec
CONSTANTS
    Sections  = {"rule", "template"}
    EntityIds = {i1, i2}
    Claims    = {c1, c2}
    Refs      = {r1, r2}
    NoClaim   = NoClaim
    ApproveKeepsOtherSections = FALSE
    MasterSupersedesById      = TRUE
INVARIANTS
    TypeOK
PROPERTIES
    ApprovePreservesSections
