\* A missing or corrupt object is accepted as a default instead of refusing.
\* ReaderHasVerifiedSnapshot MUST go red.
SPECIFICATION Spec
CONSTANTS
    Creators = {alice, bob}
    NoBook = noBook
    ConditionalPublication = TRUE
    RequireComplete = TRUE
    KeepGeneration = TRUE
    RefuseIncomplete = FALSE
    GrantOnlyPublished = TRUE
INVARIANTS
    TypeOK
    ReaderHasVerifiedSnapshot
