{
  "name": "Ratio EAC / forecast pack",
  "book_kind": "PROJECT",
  "issue": 169,
  "workos_connect": {
    "application_type": "oauth",
    "trust": "first-party",
    "flow": "authorization_code",
    "docs": "https://workos.com/docs/authkit/connect/oauth",
    "catalog": "docs/connect-scopes.md",
    "scopes": [
      "budget:read",
      "billing:read",
      "statements:read"
    ],
    "refused_aliases": [
      "journal:append",
      "journal:read",
      "projects:budget:read",
      "projects:billing:read"
    ]
  },
  "forecast_journals": {
    "status": "refused",
    "note": "The issue body still says journal:append. That string is an alias and is refused. Canonical write grant would be journals:post — but the catalog has no forecast template, and posting project_cost* as a forecast would mix a what-if into the journal of record. This app is read-only relative to the journal: it emits CSV/JSON. Empty journals:post allowlist would refuse every post. Do not invent a write scope."
  },
  "eac_compute": {
    "status": "outside the journal",
    "formula": "remaining_to_spend = revised − incurred − awarded; EAC = incurred + remaining_to_spend + awarded (= revised) when that cut is supported; ETC = remaining_to_spend + awarded",
    "note": "Unset remaining-to-spend stays unset — never a silent forecast of 0. Treating awarded as 0 would print budget − actual as headroom. A posted 0.00 is a real zero. /budget still does not forecast. No CPI / percent-complete EAC (that is a rounded figure)."
  },
  "grant_path": {
    "status": "not built",
    "note": "In-process /v1 accepts Connect catalog scopes after membership. Leftover on issue 22: API Gateway JWT authorizer still AuthKit-session only — a live Connect token can still 401 at the edge. Write-route actor binding landed (#151). This file leaves #150 and #169 open. It does not reopen #151. It leaves #163 (Personal cash forecast), #170, #172, #173, and #184 open."
  }
}
